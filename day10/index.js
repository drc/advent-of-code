const { getFile } = require("../utils");

const part1 = async file => {};

const part2 = async file => {};

Promise.all([part1("sample.txt"), part2("sample.txt")]).then(answers =>
    console.log(answers)
);
